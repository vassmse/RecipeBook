<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RecipeBook"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:AppStudio.Uwp.Controls"
    x:Class="RecipeBook.Views.MainPage"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20" />
            <RowDefinition Height="20" />
            <RowDefinition Height="20" />            
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding RecipeBook.Recipes[0].Name}"  Height="20px" Width="200px" Grid.Row="0" />
        <TextBlock Text="{Binding RecipeBook.Recipes[0].Description}"  Height="20px" Width="200px" Grid.Row="1" />
        <TextBlock Text="{Binding RecipeBook.Recipes[0].Ingredients[0].Name}"  Height="20px" Width="200px" Grid.Row="2" />
    </Grid>
    

</Page>

    
